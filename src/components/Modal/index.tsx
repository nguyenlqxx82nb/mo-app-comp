import ModalContainer, {IModal, pushModal} from './Container';
import NotificationModal  from './Notification';
import WrapModal from './WrapModal';

export { 
  ModalContainer,
  NotificationModal,
  WrapModal,
  IModal,
  pushModal
}